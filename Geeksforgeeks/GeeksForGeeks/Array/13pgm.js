//Different Ways to Delete an Item from an Array using JavaScript
/**
 * splice
 * pop - removes at end
 * shift - remove at start
 * filter - it can return only those which satisfies the condition
 * delete - can be deleted using object
 */
var ar = [23,67,21,78,34]
console.log(ar.pop())
console.log(ar.shift())