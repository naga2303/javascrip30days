// indexOf(): Takes takes a substring and if the substring exists in a string it returns the first position of the substring if does not exist it returns -1
let strIndex="Nagarajan"
console.log(strIndex.indexOf('N'))
console.log(strIndex.indexOf('Nagarajan'))
console.log(strIndex.indexOf('j'))
console.log(strIndex.indexOf('A'))